import {NextResponse} from "next/server"; import Twilio from "twilio"; import {createAdminClient} from "@/lib/supabase/admin";
export async function POST(req:Request){const token=process.env.TWILIO_AUTH_TOKEN;if(!token)return NextResponse.json({error:"Twilio not configured"},{status:503});const url=process.env.TWILIO_STATUS_CALLBACK_URL||req.url;const params=Object.fromEntries(new URLSearchParams(await req.text()));const signature=req.headers.get("x-twilio-signature")||"";if(!Twilio.validateRequest(token,signature,url,params))return NextResponse.json({error:"Invalid signature"},{status:401});const sid=String(params.MessageSid||"");const status=String(params.MessageStatus||"unknown");const db=createAdminClient();await db.from("communication_logs").update({status,provider_status:params}).eq("provider_message_id",sid);return new NextResponse(null,{status:204});}
