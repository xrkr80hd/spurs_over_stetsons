import {redirect} from "next/navigation"; import {PageHeading} from "@/components/page-heading"; import {requireViewer,isStaff} from "@/lib/auth"; import {createClient} from "@/lib/supabase/server"; import {saveClassType} from "../actions";
export default async function Page(){const viewer=await requireViewer();if(!isStaff(viewer.roles))redirect("/dashboard");const db=(await createClient())!;const {data}=await db.from("class_types").select("*").order("name");return <><PageHeading title="Class types" description="Reusable class definitions, durations, capacity, and pricing."/><div className="grid gap-5 lg:grid-cols-[360px_1fr]"><form action={saveClassType} className="panel grid content-start gap-4 p-5"><h2 className="western text-2xl font-bold">Add class type</h2><div className="field"><label>Name</label><input name="name" required/></div><div className="field"><label>Level</label><input name="level" placeholder="Beginner, all levels…"/></div><div className="field"><label>Duration (minutes)</label><input name="duration_minutes" type="number" min="15" defaultValue="60" required/></div><div className="field"><label>Default capacity</label><input name="default_capacity" type="number" min="1" defaultValue="20" required/></div><div className="field"><label>Default price</label><input name="price" type="number" min="0" step="0.01" defaultValue="0" required/></div><button className="btn btn-primary">Create class type</button></form><div className="panel table-wrap p-4"><table><thead><tr><th>Class</th><th>Level</th><th>Duration</th><th>Capacity</th><th>Price</th></tr></thead><tbody>{data?.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.level}</td><td>{x.duration_minutes} min</td><td>{x.default_capacity}</td><td>{(x.default_price_cents/100).toLocaleString("en-US",{style:"currency",currency:"USD"})}</td></tr>)}</tbody></table></div></div></>}
