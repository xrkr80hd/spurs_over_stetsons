import type {Metadata} from "next"; import {publicMedia} from "@/lib/data";
export const metadata:Metadata={title:"Media"};
export default async function Media(){const data=await publicMedia();return <section className="wrap py-14"><h1 className="western text-5xl font-bold">Media</h1><p className="mb-8 mt-3 text-[var(--muted)]">Highlights from classes, workshops, and the dance floor.</p><div className="grid gap-5 md:grid-cols-2">{data.map(x=><article key={x.id} className="panel overflow-hidden"><div className="grid aspect-video place-items-center bg-[#171310] text-5xl">{x.media_type==="video"||x.media_type==="youtube"?"▶":"✦"}</div><div className="p-5"><h2 className="western text-2xl font-bold">{x.title}</h2><p className="mt-2 text-[var(--muted)]">{x.description}</p></div></article>)}{!data.length&&<div className="empty md:col-span-2">Media coming soon.</div>}</div></section>}
