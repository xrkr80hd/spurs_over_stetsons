import Twilio from "twilio"; import type {SendResult} from "./email";
export async function sendSms(input:{to:string;message:string}):Promise<SendResult>{const sid=process.env.TWILIO_ACCOUNT_SID;const token=process.env.TWILIO_AUTH_TOKEN;const from=process.env.TWILIO_PHONE_NUMBER;if(!sid||!token||!from)return {ok:false,error:"Twilio is not configured."};try{const client=Twilio(sid,token);const msg=await client.messages.create({to:input.to,from,body:input.message,statusCallback:process.env.TWILIO_STATUS_CALLBACK_URL||undefined});return {ok:true,providerId:msg.sid};}catch(e){return {ok:false,error:e instanceof Error?e.message:"SMS provider error"};}}
