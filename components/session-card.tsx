import Link from "next/link";
type Session={id:string;starts_at:string;ends_at:string;capacity:number;price_cents:number;location:string|null;class_types:{name:string;level:string|null}|null;instructors:{public_name:string}|null;registrations?:{status:string}[]};
export function SessionCard({session}:{session:Session}){const confirmed=session.registrations?.filter(r=>r.status==="confirmed").length||0;const full=confirmed>=session.capacity;return <article className="panel flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div><p className="mb-1 text-xs font-bold uppercase tracking-[.15em] text-[var(--accent2)]">{new Date(session.starts_at).toLocaleDateString("en-US",{weekday:"long",month:"short",day:"numeric"})}</p><h2 className="western text-2xl font-bold">{session.class_types?.name || "Dance Class"}</h2><p className="mt-1 text-sm text-[var(--muted)]">{new Date(session.starts_at).toLocaleTimeString("en-US",{hour:"numeric",minute:"2-digit"})} · {session.instructors?.public_name || "Instructor TBA"} · {session.location || "Main dance floor"}</p><p className="mt-2 text-sm">{full ? "Class full — waitlist available" : `${session.capacity-confirmed} spots available`} · {(session.price_cents/100).toLocaleString("en-US",{style:"currency",currency:"USD"})}</p></div><Link className="btn btn-primary" href={`/register?session=${session.id}`}>{full?"Join waitlist":"Book class"}</Link></article>}
