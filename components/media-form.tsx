"use client"; import {useState} from "react";
export function MediaForm(){const [result,setResult]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setResult("Uploading…");const res=await fetch("/api/media/upload",{method:"POST",body:new FormData(e.currentTarget)});const json=await res.json();setResult(res.ok?"Media saved.":json.error||"Upload failed.");if(res.ok)location.reload();}return <form onSubmit={submit} className="panel grid gap-4 p-5 md:grid-cols-2"><div className="field"><label>Title</label><input name="title" required/></div><div className="field"><label>Type</label><select name="mediaType"><option value="photo">Photo</option><option value="video">Video</option><option value="youtube">YouTube</option><option value="placeholder">Placeholder</option></select></div><div className="field md:col-span-2"><label>File (photo/video)</label><input name="file" type="file" accept="image/jpeg,image/png,image/webp,video/mp4,video/webm"/></div><div className="field md:col-span-2"><label>External/YouTube URL</label><input name="externalUrl" type="url"/></div><div className="field md:col-span-2"><label>Description</label><textarea name="description"/></div><div className="field"><label>Status</label><select name="status"><option value="draft">Draft</option><option value="published">Published</option><option value="scheduled">Scheduled</option></select></div><div className="field"><label>Publish at</label><input name="publishedAt" type="datetime-local"/></div><button className="btn btn-primary md:col-span-2">Save media</button>{result&&<p className="md:col-span-2">{result}</p>}</form>}
