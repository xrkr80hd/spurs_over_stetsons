"use client"; import {useState} from "react";
export function InviteUserForm(){const [result,setResult]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setResult("Inviting…");const res=await fetch("/api/users/invite",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});const json=await res.json();setResult(res.ok?"Invitation sent.":json.error||"Invitation failed.");}return <form onSubmit={submit} className="panel grid gap-4 p-5 md:grid-cols-2"><div className="field"><label>Full name</label><input name="fullName" required/></div><div className="field"><label>Email</label><input name="email" type="email" required/></div><div className="field"><label>Phone</label><input name="phone" type="tel"/></div><div className="field"><label>Role</label><select name="role"><option value="student">Student</option><option value="instructor">Instructor</option><option value="manager">Manager</option></select></div><button className="btn btn-primary md:col-span-2">Send secure invitation</button>{result&&<p className="md:col-span-2">{result}</p>}</form>}
